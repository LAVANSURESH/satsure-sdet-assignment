# Runtime + test dependencies for the Autocomplete Form assignment.
# Pinned to the versions the suite was developed and verified against.

flask==3.1.3
pytest==9.1.1
playwright==1.62.0
pytest-playwright==0.9.0
pytest-html==4.2.0
requests==2.34.2
jsonschema==4.26.0
