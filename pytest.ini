[pytest]
testpaths = tests
addopts =
    -ra -q
    --screenshot=only-on-failure --video=retain-on-failure --tracing=retain-on-failure --output=test-results
    --html=report.html --self-contained-html
markers =
    ui: browser tests driven through Playwright
    api: REST API contract tests
